# WASM build configuration
[target.wasm32-unknown-unknown]
rustflags = [
    '-C', 'link-arg=--initial-memory=33554432',  # 32MB initial memory (512 pages × 64KB)
    '-C', 'link-arg=--max-memory=67108864',      # 64MB max memory (1024 pages × 64KB)
]
